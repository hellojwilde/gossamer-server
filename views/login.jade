extends _modal

block content
  .panel-body
    .alert.alert-info(role='alert').
      Looks like you need to log in to see this.
      Don't worry, we'll get you going in a jiffy.

    a.btn.btn-default.btn-lg.btn-primary.btn-block(href=url).
      Login with Github